# add a version and change to it
git checkout -b dev
#show the version
git branch
#switch version
git checkout versionname
--------
#show remotename
git remote
#??how to set remote?

#push to remote
git push remotename localname
